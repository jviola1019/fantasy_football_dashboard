# Full Audit & Reverification — 2026-06-01

**Model:** Claude Opus 4.8 (`claude-opus-4-8`)
**Scope:** All sprints, account, security, rosters/rookies, validations, backtesting,
and documentation accuracy ("no context rot"). Every number below was reproduced
from a command run in this session, not copied from a prior report.

---

## 1. Gates re-executed (ground truth)

| Gate | Command | Result |
| --- | --- | --- |
| TypeScript | `npx tsc --noEmit` | **PASS** (exit 0) |
| ESLint | `npx eslint .` | **PASS** (0 errors, 0 warnings) |
| Vitest | `npx vitest run` | **PASS** 385 passed / 4 skipped (389), 42 files |
| Build | `npx next build` | **PASS** — 17 routes (2 static, 15 dynamic) |
| E2E (prev. run, same code) | `npx playwright test` | **PASS** 48/48 (chromium + mobile-chrome) |
| Live backtest | `npx tsx scripts/brier-backtest.ts` | **PASS** — reproduced exactly |

The 4 skipped vitest specs are the live-API round-trips (`sleeper.live`,
`espn.live`, `trade/values.live`), gated by `RAE_LIVE_TESTS=1`.

## 2. Backtest reproduces exactly (live 2025 Sleeper)

Re-ran the prospective Brier backtest against live Sleeper 2025 projections vs.
actuals. Output matched `docs/brier-results.md` byte-for-byte:

- 3,573 startable player-weeks across 17 weeks (218 DNP scored 0).
- QB discrim Brier **0.2716** (n=512), RB **0.1981** (n=876), WR **0.2103** (n=1470), TE **0.2147** (n=715).
- Calibration model ECE 0.017–0.033 (vs the rank model's 0.05–0.17).

## 3. Rosters / incoming rookies — VERIFIED UP-TO-DATE

The owner's requirement: rosters & the draft pool must include incoming (2026)
rookies. The live data path is **year-agnostic** (only the labeled demo fixture
carries a fixed date), so it reflects the current player universe:

- **Sleeper player map** (`/v1/players/nfl`, 12,190 players): **223** incoming
  rookies (`years_exp === 0`, skill position, on an NFL team) — e.g. Fernando
  Mendoza, Carson Beck, Ty Simpson, Jeremiyah Love, Carnell Tate, Jordyn Tyson
  (high ids 13268+). Rosters materialize from this map, so any rostered rookie
  appears. `normalize.ts` does not filter rookies (only requires a valid position).
- **FantasyPros consensus draft pool** (the mock-draft source, `fpDataToRecords`):
  **455** players including the 2026 rookie class — Jeremiyah Love #34, Carnell
  Tate #65, Jordyn Tyson #90, Fernando Mendoza #217, Ty Simpson #333, Carson Beck
  #353, plus Jeanty/Hampton/Judkins. `/mock-draft` reads this snapshot (honest
  "not cached yet" fallback when empty).
- **Projections cron** uses `getNflState()` → season **2026** (dynamic, not pinned).

Operational note: the live universe is current; on a deployed instance the daily
crons (`players-refresh`, `rankings-refresh`) keep the cached snapshots fresh, and
`loadRAEEnvelope` falls back to a live fetch when a snapshot is > 36h old.

## 4. Account & security — intact

- crypto AES-256-GCM round-trip + tamper rejection, scrypt password hashing,
  per-user league/notification isolation: **19 tests pass** (`crypto`, `passwords`,
  `leagues`).
- Prior session's security hardening (constant-time `safeEqual` on all 7 token
  gates, sanitized `addLeague` error) shipped and CI-green at `84a3957`.
- No `NEXT_PUBLIC_*`, no `dangerouslySetInnerHTML`, no `console.*` in `src`, no
  secrets in client bundles (re-confirmed by grep + the prior exposure audit).

## 5. Honesty fix applied this session

`Market Intelligence → Trends → VolatilitySurface` still rendered the **old
flat risk-tolerance heatmap** (Champ/Playoff/Chaos × risk), whose rows came out
dead-flat (Playoff 100/100/100, Chaos 0/0/0) for any non-marginal team — the same
defect already fixed in the Nexus Simulator. Extracted `buildOutcomeHeat` into
`src/lib/outcomeHeat.ts` and pointed **both** panels at the real **joint
(wins × playoff-outcome) distribution** (grid sums to 100%; intensity = true
likelihood). Verified visually (MI Trends tab now shows the wins×outcome grid with
a "Most likely: N wins → tier (P%)" caption).

## 6. Context rot corrected (living docs)

| File | Was | Now |
| --- | --- | --- |
| `README.md` badge + testing section | "374" / "208" tests | **385** tests (4 live skipped) |
| `README.md` Nexus screenshot caption + design bullet | "outcomes across risk tolerance" / "per-risk-tolerance runs" | joint wins × playoff-outcome distribution (`outcomeHeat.ts`) |
| `README.md` known-limitations | "Sleeper user_id not captured → cron scores first roster" | optional **Sleeper username** captured → `resolveSleeperRosterId` scores the actual team |
| `README.md` FAAB | "activates when surfaced" | wired end-to-end for Sleeper (`extractSleeperFaabRatio` → cron → rules) |
| `TASKS.md` "Still deferred" | FAAB + user_id capture listed as future | moved to **Phase 9 (shipped)**; only ESPN-FAAB + Yahoo + live-homepage remain |

Historical ledgers (`reports/opus48-reverification.md`, `final-audit-report.md`,
`audit_report.md`, `FINAL_AUDIT.md`) are point-in-time records and were **not**
rewritten — this file is the current snapshot.

## 7. Sprint status

Phases 1–8 (TASKS.md) remain shipped. Phase 9 (real season Monte Carlo, honest
joint heatmaps, full FantasyPros draft pool, Sleeper FAAB + identity, modernized
tabs, security hardening) is shipped and reflected in TASKS.md. Genuine remaining
work: Yahoo OAuth adapter, paid feeds, ESPN-FAAB extraction, auto-load homepage.

*Generated by Claude Opus 4.8, 2026-06-01.*
